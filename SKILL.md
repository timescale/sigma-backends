---
name: sigma-backends
description: "Retired. This skill is archived and must not be used to convert Sigma rules or run rsigma. For Sigma YAML authoring (detection, correlation, filters, pipelines, modifiers), use the sigma-rules skill. For the rsigma CLI and MCP server, use the rsigma skill from timescale/rsigma."
---

# Retired

This skill is archived. It taught rsigma commands that no longer exist.

- Sigma YAML authoring: [sigma-rules](https://github.com/timescale/sigma-rules). Install with `npx skills add timescale/sigma-rules -g -y`.
- rsigma CLI and MCP: the [rsigma](https://github.com/timescale/rsigma) skill, published from `skills/rsigma/`. Install with `npx skills add timescale/rsigma -g -y`.
